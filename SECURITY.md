# 보안

## 취약점 신고

**공개 이슈로 올리지 마세요.** GitHub 의
[Security Advisory](https://github.com/TOKTOKHAN-DEV/agent-company/security/advisories/new)
로 비공개 신고해 주세요.

이 저장소는 서비스가 아니라 템플릿입니다. 그래서 위험은 대부분 **이걸로 만든 프로젝트가
키를 어떻게 다루느냐**에서 옵니다. 아래는 그 지점들입니다.

## 이 저장소가 지키는 것

**`inbox/` 는 실행하지 않습니다.** `pnpm intake` 로 받은 zip · tar.gz · 폴더는 읽을거리이지
작업 대상이 아닙니다. `npm install` 도, 빌드도, 스크립트 실행도 하지 않습니다. 심볼릭 링크는
풀 때 지웁니다. **키로 보이는 파일은 지우지 않고 목차(`INVENTORY.md`)에 경고로 올립니다** —
조용히 지우면 받은 사람이 키가 섞여 있었다는 사실 자체를 모르게 됩니다.

`inbox/` 는 `.gitignore` 에 있습니다. 남이 준 자료가 실수로 커밋되지 않습니다.

**에이전트에는 쓰기 범위가 있습니다.** `agents/registry.yaml` 의 `writes:` 밖은 건드리지
않습니다. 범위가 겹치지 않는 에이전트만 동시에 돌립니다.

**출고는 사람만 누릅니다.** 에이전트는 `in_review` 까지만 올립니다. 판단은 이름이 아니라
**부작용**으로 합니다 — setter 처럼 보이지만 검토 요청이 함께 나가는 API 가 있습니다.

## 이걸로 만든 프로젝트에서 조심할 것

**`.env` 를 커밋하지 마세요.** `.env.example` 만 저장소에 둡니다. 실제 값은 `.env` 에 두고,
CI·배포는 각 플랫폼의 시크릿 저장소를 씁니다.

**메모리와 wiki 에 키를 적지 마세요.** `wiki/memory/` 는 세션마다 컨텍스트로 주입되고 저장소에
커밋됩니다. 근거와 경로를 남기는 곳이지 값을 남기는 곳이 아닙니다.

**`.vercel/` 을 커밋하지 마세요.** `projectId` 와 `orgId` 가 계정에 묶여 있습니다.
루트 `.gitignore` 가 덮고 있습니다.

**에이전트에게 준 CLI 의 권한은 그대로 상속됩니다.** `pnpm agent` 는 `claude` 나 `codex` 를
띄웁니다. 그 CLI 가 접근할 수 있는 것은 에이전트도 접근할 수 있습니다. 자격 증명이 든
디렉터리에서 띄우지 마세요.

## 범위 밖

`claude` · `codex` · `gh` 같은 외부 CLI 자체의 취약점은 각 프로젝트에 신고해 주세요.
이 저장소는 그것들을 호출할 뿐입니다.
